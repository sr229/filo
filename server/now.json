{
    "version": 2,
    "builds": [
        {
            "src": "index.ts",
            "use": "@now/node"
        }
    ],
    "routes": [
        { "src": "/(?<url>[^*]+)", "dest": "/?url=$url", "methods" : ["GET"]}
    ]
}