{
    "version": 2,
    "builds": [
        {
            "src": "**/*.ts",
            "use": "@now/node"
        }
    ],
    "routes": [
        { "src": "/(?<url>[^*]+)", "dest": "/?url=$url", "methods" : ["GET"]}
    ]
}